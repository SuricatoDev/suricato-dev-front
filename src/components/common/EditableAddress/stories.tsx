import { Meta, StoryObj } from '@storybook/react'
import EditableAdress from '.'

export default {
  title: 'EditableAdress',
  component: EditableAdress
} as Meta

export const Default: StoryObj = {}
