import { Meta, StoryObj } from '@storybook/react'

import Divider from '.'

export default {
  title: 'Components/Divider',
  component: Divider,
  tags: ['autodocs']
} as Meta

export const Default: StoryObj = {}
