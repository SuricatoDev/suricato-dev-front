import { Meta, StoryObj } from '@storybook/react'
import BackToTopWithProgress from '.'

export default {
  title: 'Components/BackToTopWithProgress',
  component: BackToTopWithProgress
} as Meta

export const Default: StoryObj = {}
